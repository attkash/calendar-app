{
  "name": "calendar-app",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "server": "node server.js",
    "web": "npm run dev --prefix client",
    "dev": "concurrently -n srv,web -c cyan,magenta \"npm run server\" \"npm run web\"",
    "build": "cd client && npm run build",
    "preview": "npm run build && node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.1",
    "puppeteer": "^24.40.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
